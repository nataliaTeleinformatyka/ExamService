{# templates/login.html.twig #}
{% extends 'base.html' %}
{% block body %}
    <body>
    {% if errors %}
        {{ errors }}
    {% endif %}
        <div class="signIn"> LOGOWANIE
        {{ form(form) }}
        </div>
    </body>
{% endblock %}

